name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: JimCronqvist/action-ssh@master
      with:
        hosts: 'ubuntu@testfileexist.py'
        privateKey: ${{ secrets.PRIVATE_KEY }}
        command: ls -lah
